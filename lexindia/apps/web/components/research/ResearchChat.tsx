'use client';
/**
 * ResearchChat — Full-page conversational research interface.
 *
 * TODO: This component is scaffolded and ready for implementation.
 * It should provide a rich chat UI wired to POST /api/v1/research/analyze
 * with streaming support, citation rendering, and session history.
 *
 * For the current MVP the main Dashboard handles search via GET /research/search.
 */
import React from 'react';
import styles from './ResearchChat.module.css';

const ResearchChat = () => {
  return (
    <div className={styles.placeholder}>
      <p>ResearchChat — coming soon.</p>
    </div>
  );
};

export default ResearchChat;
